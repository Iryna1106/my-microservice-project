apiVersion: v2
name: django-app
description: A Helm chart that deploys the Django application to Kubernetes (EKS).
type: application

# Version of THIS chart (bump it when you change the templates).
version: 0.1.0

# Version of the APP being deployed (informational).
appVersion: "1.0.0"
